{
  "name": "gui-rocha-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Portal pessoal e catálogo de produtos de Guilherme Rocha.",
  "type": "module",
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "check": "node --check assets/js/site.js && node --check assets/js/home.js && node --check assets/js/project.js && node --check assets/js/contact.js && node --check service-worker.js && node --check scripts/serve.mjs && node --check scripts/build.mjs && node --check scripts/validate.mjs && npm run validate && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
